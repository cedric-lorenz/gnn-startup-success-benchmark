# Exact-pinned dependencies for the versions used to produce all paper
# results (py3117 conda env + venv). Pins satisfy the Pineau v2.0
# reproducibility checklist requirement for a precise dependency spec.
#
# Install PyTorch and PyTorch Geometric first, matching your CUDA build
# (the version strings below are CUDA-agnostic; pick the matching CUDA wheel
# from the official indices):
#   https://pytorch.org/get-started/locally/
#   https://pytorch-geometric.readthedocs.io/en/latest/install/installation.html
torch==2.6.0
torch-geometric==2.6.1

# Graph & ML
captum==0.8.0
networkx==3.4.2
python-louvain==0.16
scikit-learn==1.6.1
scipy==1.13.1
xgboost==3.1.2

# NLP
sentence-transformers==5.1.2

# Data & config
numpy==1.26.4
pandas==2.3.1
PyYAML==6.0.2

# Visualization
adjustText==1.3.0
matplotlib==3.10.3
seaborn==0.13.2
pyvis==0.3.2

# Utilities
rapidfuzz==3.13.0
tqdm==4.67.1
wandb==0.20.1

# Testing
pytest==9.0.2

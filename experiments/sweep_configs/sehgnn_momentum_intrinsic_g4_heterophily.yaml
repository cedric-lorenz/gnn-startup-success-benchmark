# =============================================================================
# Bayesian sweep: SeHGNN on Momentum
# =============================================================================
# Target: Joint (mom + liq)/2 AUC-PR — unified ICDM selection metric
# Selection metric: val_auc_pr_joint (maximize)
# Search: 18 hyperparameters | Fixed: 4 model/target choices
# Generated by scripts/generate_sweep_configs.py — do not edit by hand,
# change SEARCH_SPACES["SeHGNN"] in that file and regenerate.
# =============================================================================

project: gnn-startup-successs
method: bayes
metric:
  name: val_auc_pr_joint
  goal: maximize
program: src/main.py
command:
- ${env}
- python
- ${program}
- ${args}
early_terminate:
  type: hyperband
  min_iter: 10
  eta: 3
parameters:
  train.lr:
    distribution: log_uniform_values
    min: 0.0005
    max: 0.02
  train.weight_decay:
    distribution: log_uniform_values
    min: 1.0e-06
    max: 0.01
  train.gradient_clip_val:
    distribution: uniform
    min: 0.5
    max: 2.0
  train.loss.label_smoothing:
    distribution: uniform
    min: 0.0
    max: 0.1
  train.loss.momentum_weight:
    distribution: uniform
    min: 0.3
    max: 2.0
  train.loss.liquidity_weight:
    distribution: uniform
    min: 0.1
    max: 2.0
  train.optimizer:
    values:
    - Adam
    - AdamW
  train.scheduler.type:
    values:
    - CosineAnnealingLR
    - ReduceLROnPlateau
    - None
  train.scheduler.warmup_epochs:
    values:
    - 0
    - 5
    - 10
    - 20
  models.SeHGNN.dropout:
    distribution: uniform
    min: 0.0
    max: 0.5
  models.SeHGNN.input_drop:
    distribution: uniform
    min: 0.0
    max: 0.3
  models.SeHGNN.att_drop:
    distribution: uniform
    min: 0.0
    max: 0.3
  models.SeHGNN.hidden_channels:
    values:
    - 64
    - 128
  models.SeHGNN.heads:
    values:
    - 2
    - 4
  models.SeHGNN.num_hops:
    values:
    - 2
    - 3
  models.SeHGNN.attention_temperature:
    distribution: uniform
    min: 0.5
    max: 2.0
  models.SeHGNN.transformer_activation:
    values:
    - relu
  models.SeHGNN.activation_type:
    values:
    - prelu
    - relu
    - elu
  models.SeHGNN.use_residual:
    value: true
  models.SeHGNN.use_retrieval_head:
    value: false
  models.SeHGNN.use_self_loop:
    value: true
  models.SeHGNN.use_discrepancy:
    value: false
  train.model:
    value: SeHGNN
  data_processing.ablation.feature_information_level:
    value: intrinsic
  data_processing.graph_variant:
    value: g4_heterophily
  data_processing.edge_loading.founder_coworking:
    value: true
  seed:
    value: 42
  train.epochs:
    value: 100
  analysis.enable_downstream_analysis:
    value: false
  analysis.enable_homophily_analysis:
    value: false
  analysis.enable_visualization_analysis:
    value: false
  explain.enabled:
    value: false
  wandb.enabled:
    value: true
